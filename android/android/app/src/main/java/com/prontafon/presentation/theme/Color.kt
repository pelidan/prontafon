package com.prontafon.presentation.theme

import androidx.compose.ui.graphics.Color

// Primary brand colors (matching Flutter design)
val Primary = Color(0xFF0057FF)  // Primary Blue
val PrimaryVariant = Color(0xFF0046CC)  // Darker blue variant
val PrimaryLight = Color(0xFF3377FF)  // Lighter blue variant
val PrimaryDark = Color(0xFF003C99)  // Even darker blue

// Secondary colors
val Secondary = Color(0xFF00C2FF)  // Accent Cyan
val SecondaryLight = Color(0xFF33D1FF)
val SecondaryDark = Color(0xFF0099CC)

// Background colors (dark theme)
val Background = Color(0xFF121212)  // Dark BG
val BackgroundDark = Color(0xFF121212)
val Surface = Color(0xFF1E1E1E)  // Slightly lighter for cards
val SurfaceDark = Color(0xFF1E1E1E)
val SurfaceVariant = Color(0xFF2A2A2A)
val SurfaceVariantDark = Color(0xFF2A2A2A)

// Status colors (semantic colors for connection states)
val Connected = Color(0xFF4CAF50)  // Keep green
val Connecting = Color(0xFFFFA726)  // Keep orange
val Disconnected = Color(0xFF9E9E9E)  // Keep gray
val Error = Color(0xFFFF3B30)  // Recording Red (used for recording state)
val ErrorDark = Color(0xFFFF3B30)
val Success = Color(0xFF4CAF50)
val Warning = Color(0xFFFFC107)

// Text colors
val OnPrimary = Color.White
val OnSecondary = Color(0xFF000000)
val OnBackground = Color.White
val OnBackgroundDark = Color.White
val OnBackgroundMuted = Color(0xB3FFFFFF) // 70% white
val OnBackgroundSubtle = Color(0x61FFFFFF) // 38% white
val OnSurface = Color.White
val OnSurfaceDark = Color.White
